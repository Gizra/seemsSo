/**
 * Plain JS equivelent of jQuery.extend()
 *
 * See: https://stackoverflow.com/a/11197343/750039
 */
function extend() {
  for (var i = 1; i < arguments.length; i++)
    for (var key in arguments[i])
      if (arguments[i].hasOwnProperty(key))
        arguments[0][key] = arguments[i][key];
  return arguments[0];
}

var yesodSettings = yesodSettings || {};
extend(yesodSettings, #{rawJS jsSettings});

console.log(yesodSettings);
